import java.util.Scanner;
public class Ans2 {
    Scanner sc = new Scanner(System.in);
    System.out.print("eter the unit of electricity:");
    int unit= sc.nextInt();
    if (unit>=50) && (unit<150){
        double up=unit*0.50;
        double tc=up+0.2*up;
        system.out.print("the total price is :"+tc)

    }
    else if (unit>=50) && (unit>=150){
        double up=unit*0.50;
        double tc=up+0.2*up;
        system.out.print("the total price is :"+tc)

    }
}
